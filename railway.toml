[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build && npm ci --prefix server"

[deploy]
startCommand = "npm start --prefix server"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
